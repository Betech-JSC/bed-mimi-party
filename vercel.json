{
  "buildCommand": "yarn install && yarn route && yarn frontend:build && yarn backend:build",
  "installCommand": "yarn install",
  "framework": null,
  "outputDirectory": "public",
  "devCommand": "yarn dev"
}

